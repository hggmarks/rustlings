// variables5.rs
// Execute `rustlings hint variables5` or use the `hint` watch subcommand for a hint.


fn main() {
    let number = "S-I-X"; // don't change this line
    {
        println!("Spell a Number : {}", number);
        let number = 6; // don't rename this variable
        println!("Number plus sixty-three is : {}", number + 63);
    }
}
